import { Inter } from 'next/font/google';
import { Lusitana } from 'next/font/google';
import { Edu_TAS_Beginner } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Edu_TAS_Beginner({
    weight: ['400', '700'],
    subsets: ['latin']
})